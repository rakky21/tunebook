<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tunebook</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/spectre.css/0.5.9/spectre.min.css"
    integrity="sha512-9RIcp1f4CE6dEuYX9085tXaEbYd1ap04d2Av1ub/dwuT33WbfbHStDdQ+shKrp5wzZzleh5DOg+7ABSnaQP/nQ=="
    crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body>
  <div class="container hero bg-gray col-12 p-2">
    <div class="hero-body-sm p-2 col-12 bg-primary">
    <header class="text-dark">
      <nav >
          <a href="/">Home</a>
        {{#if loggedIn}}
        <a href="/dashboard">dashboard</a>
        {{!-- <button >logout</button> --}}
    <button id="logout" class="float-right btn-no-style btn btn-primary btn-lg">Logout</button>

        {{else}}
        <a href="/login" class="float-right btn btn-primary btn-lg">login</a>
        {{/if}}
              </nav>
        <h1>
          <a href="/">Tunebook</a>
        </h1>
        </div>
    </header>
  </div>

    <main>
      {{{ body }}}
    </main>
    <footer>
      Thanks for visiting!
    </footer>
  </div>

  {{#if loggedIn}}
  <script src="/javascript/logout.js"></script>
  {{/if}}
</body>

</html>